<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item android:gravity="fill" android:drawable="@mipmap/splash"/>
    </style>

</resources>
